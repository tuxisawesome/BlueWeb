{
  "schema": 1,
  "id": "blueobject",
  "name": "BlueObject",
  "kind": "system",
  "category": "system",
  "summary": "The calculator companion this store installs through.",
  "description": "BlueObject runs on the calculator and does the installing. It talks to this page over the USB cable, writes the variables, and keeps the record of what is installed and at what version.\n\nIt updates itself through BLUEUP, a second program small enough to be rebuilt from nothing. A program on this calculator runs inside its own variable and cannot overwrite itself, so the two install each other.",
  "homepage": "https://github.com/tuxisawesome/BlueObject",
  "channels": {
    "release": "1.3.0",
    "development": "2.0.1"
  },
  "builds": {
    "2.0.1": {
      "dependencies": [],
      "notes": "Needs no C libraries at all. It carries the two USB drivers it cannot do without and loads them itself, so it runs on a calculator with nothing else installed.",
      "description": "BlueObject runs on the calculator and does the installing. It talks to this page over the USB cable, writes the variables, and keeps the record of what is installed and at what version.\n\nIt needs nothing else on the calculator. It used to load five of the shared libraries and so could not start without them, which was awkward: BlueObject is what installs things, so a calculator without them could not use this page to get them. Three of the five it now does itself, and the two USB drivers it carries inside itself.\n\nIt updates itself through BLUEUP, a second program small enough to be rebuilt from nothing. A program on this calculator runs inside its own variable and cannot overwrite itself, so the two install each other."
    },
    "1.3.0": {
      "dependencies": [
        "clibs"
      ],
      "notes": "Loads five of the shared libraries, so the C Libraries have to be installed before it will start.",
      "description": "BlueObject runs on the calculator and does the installing. It talks to this page over the USB cable, writes the variables, and keeps the record of what is installed and at what version.\n\nIt is itself a C program and loads five of the shared libraries -- graphics, the keypad, files, serial and USB -- so the C Libraries have to be on the calculator for it to start at all. Removing them stops BlueObject running, and BlueObject is what installs things, so that is not a hole you can climb out of from here.\n\nIt updates itself through BLUEUP, a second program small enough to be rebuilt from nothing. A program on this calculator runs inside its own variable and cannot overwrite itself, so the two install each other."
    }
  },
  "dependencies": [],
  "actions": {
    "install": [
      {
        "do": "upload",
        "file": "BLUEUP.8xp",
        "archive": true
      },
      {
        "do": "upload",
        "file": "BLUE.8xp",
        "archive": true
      },
      {
        "do": "message",
        "when": "post",
        "level": "action",
        "text": "Quit BlueObject and run prgmBLUEUP once to finish the update. Until you do, the calculator keeps running the version it started with."
      }
    ],
    "uninstall": [
      {
        "do": "message",
        "when": "pre",
        "level": "action",
        "text": "Removing BlueObject leaves nothing on the calculator that can install or remove anything else. Everything already installed keeps working, but this page will not be able to reach it again until BLUE.8xp is sent back with TI Connect CE."
      }
    ]
  }
}
